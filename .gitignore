.vagrant
harbor.txt

# Topologie locale : partir de lab.env.example (cp lab.env.example lab.env)
/lab.env

# Projet Terraform Proxmox : lab local, non versionné (state, secrets, images, tfvars)
/proxmox

# Addon databasement : local uniquement. values.yaml porte une clé applicative
# Laravel (appKey) en clair — le dossier n'était que « untracked », donc un
# `git add -A` l'aurait commitée.
/_k8s/databasement/

# Talos
iso/
_out/
kubeconfig
talosconfig

# Documentation générée : reconstruite par `make docs` en local et par la GitHub
# Action à chaque push sur main (~500 Ko, inutile de la versionner).
/docs/index.html
/_site/
