.vagrant
harbor.txt

# Local topology: start from lab.env.example (cp lab.env.example lab.env)
/lab.env

# Proxmox Terraform project: local lab, not versioned (state, secrets, images, tfvars)
/proxmox

# databasement addon: local only. Its values.yaml carries a Laravel application
# key (appKey) in the clear — the directory was merely "untracked", so a
# `git add -A` would have committed it.

# Talos
iso/
_out/
kubeconfig
talosconfig

# Generated documentation: rebuilt by `make docs` locally and by the GitHub
# Action on every push to main (~500 KB, no point versioning it).
/docs/index.html
/_site/
